// Popup entry point. Intentionally minimal for the walking skeleton — the
// enable toggle and blocklist management arrive in later tickets. This file
// exists so the TypeScript → dist build pipeline is exercised end to end.
function init(): void {
  // No behaviour yet.
}

document.addEventListener("DOMContentLoaded", init);
